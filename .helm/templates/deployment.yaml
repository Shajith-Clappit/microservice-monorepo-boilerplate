apiVersion: apps/v1
kind: Deployment
metadata:
  name: microservice-monorepo-boilerplate-master-default-dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: microservice-monorepo-boilerplate-master-default-dev
  template:
    metadata:
      labels:
        app: microservice-monorepo-boilerplate-master-default-dev
    spec:
      containers:
        - name: service1
          image: {{ .Values.werf.image.service1 }}
          ports:
            - containerPort: 80
        - name: service2
          image: {{ .Values.werf.image.service2 }}
          ports:
            - containerPort: 80
        - name: static-file-server
          image: {{ .Values.werf.image.service3 }}
          ports:
            - containerPort: 80